<% @purposes.each do |t| %>
  <p>
    <%= t.user_purpose %>
    <%= link_to image_tag("trash_can2", class:"img_size"), purpose_path(t), method: :delete %>
  </p>
<% end %>

<p> 
  <span class="back_btn">
    <%= link_to "戻る", :back, class:"link" %>
  </span>
  <span class="sub_btn">
    <%= link_to '目的を追加する', new_purpose_path, class:"link" %>
  </span>
</p>
