<% @comments.each do |c| %>
  <p>
    <%= c.user_comment %>
    <%= link_to image_tag("trash_can2", class:"img_size"), comment_path(c), method: :delete %>
  </p>
<% end %>

<span class="back_btn">
  <%= link_to "戻る", :back, class:"link" %>
</span>